const colors = {
  light: "#FFF",
  primary: "#532679",
  gray: "#EFEFEF",
  grayBorder: "#DDD",
  error: "#F00"
};

const fontSizes = {
  big: "24px",
  medium: "20px",
  small: "16px",
  tiny: "12px"
};

const config = {
  colors,
  fontSizes
};

export default config;
