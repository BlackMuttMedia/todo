{
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "start": "node start.js",
    "install": "cd packages/api && yarn install"
  },
  "devDependencies": {
    "shelljs": "^0.8.1"
  }
}